name: Setup Node and dependencies
description: Node 22 with npm cache and npm ci. Shared by CI and Claude agent workflows.

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v5
      with:
        node-version: 22
        cache: npm

    - run: npm ci
      shell: bash
